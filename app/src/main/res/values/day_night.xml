<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Estilo base de la aplicación -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Personaliza tu tema aquí -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <!-- Estilo para el modo oscuro -->
    <style name="AppTheme.Dark" parent="Theme.AppCompat.DayNight">
        <item name="android:background">#26262b</item>
        <!-- Agrega otros atributos específicos del modo oscuro aquí -->
    </style>
</resources>
